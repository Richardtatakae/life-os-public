/**
 * domain/index.ts — Barrel re-export for all domain modules.
 * Import from '@/server/domain' for ergonomic access.
 */

export {} // keeps this a valid module if all re-exports below are stripped
